# requirements.txt — Jetson Orin (JetPack 5.x / 6.x)
# Install order matters on Jetson — see setup.sh

# Core ML
ultralytics>=8.2.0
onnxruntime-gpu>=1.17.0      # installed from JetPack wheel, see setup.sh
torch>=2.1.0                 # use JetPack wheel, not PyPI
torchvision>=0.16.0          # use JetPack wheel

# Tracking
# pinned: newer versions dropped flat ByteTrack re-export;
#  see detect.py _init_bytetrack if upgrading
boxmot>=10.0.43,<18.1

# Face
insightface>=0.7.3
faiss-cpu>=1.7.4             # or faiss-gpu if available for your JetPack

# Audio
tflite-runtime>=2.14.0       # from tensorflow-jetson wheel, see setup.sh
sounddevice>=0.4.6

# Alerting
# Pinned to v2; code uses mqtt.CallbackAPIVersion.VERSION1 for v1 callback signatures.
paho-mqtt>=2.1.0,<3.0

# Utilities
numpy>=1.24.0
opencv-python-headless>=4.8.0
huggingface_hub>=0.20.0
cryptography>=42.0           # cam2 login (X25519 + AES-GCM); aarch64 wheels on PyPI

# Dev / test (pure-Python, safe to install from PyPI on Jetson)
pytest>=8.0.0
